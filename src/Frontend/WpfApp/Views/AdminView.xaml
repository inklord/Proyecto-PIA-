<Page x:Class="WpfApp.Views.AdminView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Gestión" Background="{StaticResource MainBackgroundBrush}">

    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Cabecera -->
        <Grid Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Panel de Administración" FontSize="28" FontWeight="Light" Foreground="{StaticResource TextPrimaryBrush}"/>
            </StackPanel>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBox x:Name="TxtSearch" Width="200" Margin="0,0,10,0" Padding="5" VerticalContentAlignment="Center"
                         TextChanged="TxtSearch_TextChanged" Tag="Buscar..."/>
                <Button Content="+ Nueva Especie" Click="BtnCreate_Click" Background="#28a745" Foreground="White" BorderThickness="0" Padding="15,8" FontWeight="Bold" Cursor="Hand"/>
            </StackPanel>
        </Grid>

        <!-- Tabla -->
        <Border Grid.Row="1" Background="White" CornerRadius="5" Padding="10">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" Color="#DDDDDD" Opacity="0.5" ShadowDepth="2"/>
            </Border.Effect>
            
            <DataGrid x:Name="GridSpecies" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EEEEEE"
                      BorderThickness="0" Background="White" RowHeight="40">
                
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="White"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="Foreground" Value="#555555"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="BorderBrush" Value="#DDDDDD"/>
                    </Style>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="10,0"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="VerticalContentAlignment" Value="Center"/>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                    <DataGridTemplateColumn Header="Imagen" Width="60">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Width="40" Height="30" CornerRadius="3" ClipToBounds="True">
                                    <Image Source="{Binding PhotoUrl}" Stretch="UniformToFill"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Nombre Científico" Binding="{Binding ScientificName}" Width="*" FontWeight="SemiBold"/>
                    <DataGridHyperlinkColumn Header="AntWiki" Binding="{Binding AntWikiUrl}" ContentBinding="{Binding AntWikiUrl}" Width="200"/>
                    
                    <DataGridTemplateColumn Header="Acciones" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Editar" Click="BtnEdit_Click" Tag="{Binding}" Margin="0,0,5,0"
                                            Background="#007ACC" Foreground="White" BorderThickness="0" Padding="8,4" FontSize="11" Cursor="Hand"/>
                                    <Button Content="Borrar" Click="BtnDelete_Click" Tag="{Binding}"
                                            Background="#DC3545" Foreground="White" BorderThickness="0" Padding="8,4" FontSize="11" Cursor="Hand"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</Page>
