<Page x:Class="WpfApp.Views.SpeciesDetailView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Detalle de Especie" Background="{StaticResource MainBackgroundBrush}">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Cabecera -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
            <Button Content="← Volver" Click="BtnBack_Click" Background="Transparent" BorderThickness="0"
                    Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,15,0" FontSize="14"/>
            <TextBlock x:Name="TxtTitle" FontSize="28" FontWeight="Light"
                       Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Contenido -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Tarjeta con imagen -->
            <Border Background="White" CornerRadius="3" Width="260" Height="260">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Direction="270"
                                      Color="#DDDDDD" Opacity="0.5"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="180"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border CornerRadius="3,3,0,0" ClipToBounds="True">
                        <Image x:Name="ImgSpecies" Stretch="UniformToFill"
                               Cursor="Hand" MouseLeftButtonUp="ImgSpecies_MouseLeftButtonUp"/>
                    </Border>

                    <StackPanel Grid.Row="1" Margin="10">
                        <TextBlock x:Name="TxtScientificName" FontWeight="Bold" FontStyle="Italic"
                                   FontSize="14" Foreground="{StaticResource TextPrimaryBrush}" TextWrapping="Wrap"/>
                        <TextBlock Margin="0,5,0,0">
                            <Hyperlink x:Name="LinkWiki" RequestNavigate="Hyperlink_RequestNavigate">
                                Ver en AntWiki
                            </Hyperlink>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Descripción -->
            <Border Grid.Column="2" Background="White" CornerRadius="3">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Direction="270"
                                      Color="#DDDDDD" Opacity="0.5"/>
                </Border.Effect>
                <ScrollViewer Margin="15" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="TxtDescription" TextWrapping="Wrap" FontSize="14"
                               Foreground="{StaticResource TextPrimaryBrush}"/>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Page>


